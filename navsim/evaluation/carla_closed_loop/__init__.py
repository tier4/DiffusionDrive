"""Closed-loop CARLA evaluation for the Bench2Drive-trained DiffusionDrive model.

Modules here must NOT import `carla` or `leaderboard` at module level; those
packages only exist inside the evaluation container. See
docs/b2d/closed_loop_eval.md.
"""
